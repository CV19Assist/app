export default (theme) => ({
  paper: {
    padding: theme.spacing(2),
    marginBottom: theme.spacing(2),
  },
  noComments: {
    fontStyle: 'italic',
  },
  divider: {
    margin: theme.spacing(2),
  },
});
